import React from 'react'
import styles from './OrderButton.module.css';

const OrderButton = () => {
    return (
        <button className={styles.buyButton} >ORDER</button>
    )
}

export default OrderButton;